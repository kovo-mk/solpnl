# Database
# Railway provides DATABASE_URL automatically when you add Postgres
DATABASE_URL=postgresql://user:password@localhost:5432/solpnl

# For local development with SQLite:
# DATABASE_URL=sqlite:///./solpnl.db

# Helius API (required for transaction fetching)
# Get your free API key at https://helius.dev
HELIUS_API_KEY=your_helius_api_key_here

# Anthropic Claude API (required for token fraud detection)
# Get your API key at https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Redis (optional - for caching and rate limiting)
# Leave as default for local Redis, or use Railway Redis in production
REDIS_URL=redis://localhost:6379

# Frontend URL (for CORS - set to your Vercel URL in production)
FRONTEND_URL=https://your-app.vercel.app

# Server (Railway sets PORT automatically)
HOST=0.0.0.0
PORT=8000
